node_modules
.next
out
build
dist
.env
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts
.vercel
.DS_Store

# Local sign-up store (contains real emails — never commit)
New User Sign-Ups.xlsx
.env*

# Stray archives
*.zip

# Large source video — served from Vercel Blob, not the repo
public/video/Minibrief.app_Main_Video.mp4
